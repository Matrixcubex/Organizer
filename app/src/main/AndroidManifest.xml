<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions without duplicates -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Organizer"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- Single Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD0gWVjolSmCvDfdo5o3shDEjVALauKDDU"/>

        <!-- Main Activity (only one declaration) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WebSearchActivity"
            android:exported="false" />
        <!-- Other activities -->
        <activity android:name=".ActivitiesActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".VoiceAssistantActivity"
            android:label="Asistente de Voz"
            android:theme="@style/Theme.Organizer" />
        <activity android:name=".utils.MapPickerActivity"
            android:theme="@style/Theme.MaterialComponents.Dialog" />
        <activity android:name=".AgendaActivity" />
        <activity android:name=".RecordatorioActivity" />
        <activity android:name=".UbicacionActivity" />
        <activity android:name=".TelefonoActivity" />
        <activity android:name=".ui.consult.ConsultActivity"
            android:label="Consultar Eventos" />

        <!-- Receiver -->
        <receiver android:name=".receiver.EventNotificationReceiver" />
    </application>
</manifest>